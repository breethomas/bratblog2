---
---

%h1.heading Fix It
%section.post-gallery
- blog.articles.each do |article|
  %figure.post-container
    %figcaption
      = link_to article.title, article
      %p
        = article.date.strftime('%b %e')
    = article.summary
